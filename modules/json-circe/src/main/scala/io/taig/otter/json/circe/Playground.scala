package io.taig.otter.json.circe

import io.taig.otter.Plain.*
import io.taig.otter as Base

object Playground:
  @main
  def run = {
    // val arr: Schema.Writer[Vector[String]] = Base.Schema.Required.Root(Base.Collection.Root(string))
    // val tpl: Schema.Writer[String] = Base.Schema.Required.Root(Base.Tuple.Root(string))
    // val x: Schema.Writer.Of[Schema[String], String] = Base.Schema.Required.Root(Base.Tuple.Root(string))
    // println(JsonEncoder(arr, Vector("asdf", "lol")))
    // println(JsonEncoder(tpl, "asdf"))
  }
