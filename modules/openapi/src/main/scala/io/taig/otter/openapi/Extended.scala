// package io.taig.otter.openapi

// final case class Extended[A](value: A, extensions: Extensions)

// object Extended:
//   given [A]: Conversion[A, Extended[A]] with
//     override def apply(a: A): Extended[A] = Extended(a, Extensions.Empty)
